version: "2"
syncs:

  leonfit-app-sync:
    src: './app'
    sync_excludes: ['bower_components/', '.gitignore', '.git/']

  leonfit-dist-sync:
    src: './dist'
    sync_excludes: ['bower_components/', '.gitignore', '.git/']
